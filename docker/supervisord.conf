# ----- Core backends you already have -----
DATABASE_URL=postgresql+psycopg://USER:PASSWORD@your-postgres-host:5432/dify
REDIS_URL=redis://:PASSWORD@redis.com:6379/0

# ----- Vector DB (use same Postgres + pgvector) -----
VECTOR_STORE=pgvector
# If Dify expects a dedicated connection string var for pgvector, keep it too:
PGVECTOR_CONNECTION_STRING=postgresql://USER:PASSWORD@your-postgres-host:5432/dify

# ----- Web <-> API wiring -----
# Your public URLs behind your existing NGINX:
CONSOLE_WEB_URL=https://your.domain
CONSOLE_API_URL=https://your.domain/api
APP_API_URL=https://your.domain/api
SERVICE_API_URL=https://your.domain/api

# Files URL base used by the UI for signed previews
FILES_URL=https://your.domain

# ----- Misc -----
SECRET_KEY=replace_with_random_base64
LOG_LEVEL=INFO
